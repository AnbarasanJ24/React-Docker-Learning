sudo: required
services:
  - docker

before_install:
  - docker build -t anbarasan/React-Docker-Learning -f Dockerfile.dev .

script:
  -  docker run anbarasan/React-Docker-Learning npm run test -- --coverage